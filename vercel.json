{
  "version": 2,
  "buildCommand": "cd frontend && pnpm install && pnpm run build:prod",
  "outputDirectory": "frontend/build",
  "installCommand": "pnpm install",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
